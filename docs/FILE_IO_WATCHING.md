# File I/O Watching

On the bottom right quadrant of the screen is the file I/O watching tab. Default output is both STDERR and STDOUT. Default input is stdin. 

## Controls

- Ctrl+F: Search for a file descriptor. Leaving it blank will set it back to default output. 
- Keyboard Input: Send input into the file descriptor currently selected.